query Post($_id: Int!) {
    post(_id: $_id) {
        _id
        createdAt
        updatedAt
        title
        points
        voteStatus
        text
        creator {
            username
            _id
        }
    }
}